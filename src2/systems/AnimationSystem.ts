import { System } from "../utils/ECS/ECS";

// class AnimationSystem implements System {
//   update(dt: number) {
//     for (const entity of entities) {
//       if (entity.animation && entity.state) {
//         const animation = entity.animation.animations[entity.state.currentState];
//         if (animation) {
//           entity.animation.currentAnimation = animation.name;
//           entity.animation.currentFrame = animation.startFrame;
//           entity.animation.timeSinceLastFrame = animation.frameDuration;
//         }
//       }
//     }
//   }
// }
